apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 0
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - image: nginx
        name: demo-container
        ports:
        - containerPort: 80
